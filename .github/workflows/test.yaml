name: CI Tests

on:
  push:
    branches:
      - "**"

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2
      with:
        go-version: "1.16.6"
    - run: make test
